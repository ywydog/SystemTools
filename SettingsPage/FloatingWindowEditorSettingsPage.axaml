<ci:SettingsPageBase x:Class="SystemTools.FloatingWindowEditorSettingsPage"
                     xmlns="https://github.com/avaloniaui"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:ci="http://classisland.tech/schemas/xaml/core"
                     xmlns:controls="clr-namespace:FluentAvalonia.UI.Controls;assembly=FluentAvalonia"
                     xmlns:controls1="clr-namespace:SystemTools.Controls"
                     xmlns:ruleset="clr-namespace:ClassIsland.Core.Controls.Ruleset;assembly=ClassIsland.Core"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Classes="settings-container animated-intro">
            <Label FontSize="20"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                SystemTools
            </Label>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <ci:FluentIcon FontSize="56"
                               Glyph="&#xEA36;"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center" />
                <Label VerticalAlignment="Center" FontSize="28">设置 · 悬浮窗</Label>
            </StackPanel>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xEA37;}"
                                       Header="显示悬浮窗">
                <controls:SettingsExpander.Footer>
                    <ToggleSwitch IsChecked="{Binding ViewModel.Settings.ShowFloatingWindow, Mode=TwoWay}"
                                  IsEnabled="{Binding ViewModel.HasFloatingTriggerEntries}"
                                  Click="OnFloatingWindowConfigChanged" />
                </controls:SettingsExpander.Footer>
            </controls:SettingsExpander>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xE9A8;}"
                                       Header="悬浮窗配置方案"
                                       Description="切换和管理悬浮窗配置方案"
                                       IsExpanded="True">
                <StackPanel Margin="12,8,12,12" Spacing="8">
                    <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                        <TextBlock Grid.Column="0"
                                   Text="{Binding ViewModel.Settings.FloatingWindowProfiles[ViewModel.Settings.FloatingWindowProfileIndex].Name}"
                                   VerticalAlignment="Center" />
                        <Button Grid.Column="1"
                                Content="切换方案"
                                Click="OnToggleFloatingWindowProfileClick" />
                    </Grid>
                    <ItemsControl ItemsSource="{Binding ViewModel.Settings.FloatingWindowProfiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,4">
                                    <TextBox Grid.Column="0"
                                             Text="{Binding Name, Mode=TwoWay}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </controls:SettingsExpander>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xE51E;}"
                                       Header="外观设置"
                                       Description="设置悬浮窗外观"
                                       IsExpanded="True">

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xE10B;}"
                                               Content="悬浮窗缩放"
                                               Description="调整悬浮窗缩放大小">
                    <controls:SettingsExpanderItem.Footer>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Slider Width="160"
                                    Minimum="0.5"
                                    Maximum="2"
                                    TickFrequency="0.1"
                                    IsSnapToTickEnabled="True"
                                    Value="{Binding ViewModel.Settings.FloatingWindowScale, Mode=TwoWay}" />
                            <TextBlock
                                Text="{Binding ViewModel.Settings.FloatingWindowScale, StringFormat='{}{0:F1}x'}"
                                Width="46"
                                VerticalAlignment="Center" />
                        </StackPanel>
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xF17E;}"
                                               Content="图标大小"
                                               Description="调整悬浮窗内图标缩放">
                    <controls:SettingsExpanderItem.Footer>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Slider Width="160"
                                    Minimum="15"
                                    Maximum="50"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    Value="{Binding ViewModel.Settings.FloatingWindowIconSize, Mode=TwoWay}" />
                            <TextBlock
                                Text="{Binding ViewModel.Settings.FloatingWindowIconSize, StringFormat='{}{0}px'}"
                                Width="46"
                                VerticalAlignment="Center" />
                        </StackPanel>
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xF17E;}"
                                               Content="文字大小"
                                               Description="调整悬浮窗内文字缩放">
                    <controls:SettingsExpanderItem.Footer>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Slider Width="160"
                                    Minimum="8"
                                    Maximum="25"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    Value="{Binding ViewModel.Settings.FloatingWindowTextSize, Mode=TwoWay}" />
                            <TextBlock
                                Text="{Binding ViewModel.Settings.FloatingWindowTextSize, StringFormat='{}{0}px'}"
                                Width="46"
                                VerticalAlignment="Center" />
                        </StackPanel>
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xF045;}"
                                               Content="透明度"
                                               Description="调整悬浮窗透明度">
                    <controls:SettingsExpanderItem.Footer>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Slider Width="160"
                                    Minimum="10"
                                    Maximum="100"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    Value="{Binding ViewModel.Settings.FloatingWindowOpacity, Mode=TwoWay}" />
                            <TextBlock Text="{Binding ViewModel.Settings.FloatingWindowOpacity, StringFormat='{}{0}%'}"
                                       Width="46"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xEA37;}"
                                               Content="主题"
                                               Description="设置悬浮窗主题">
                    <controls:SettingsExpanderItem.Footer>
                        <ComboBox SelectedIndex="{Binding ViewModel.Settings.FloatingWindowTheme, Mode=TwoWay}">
                            <ComboBoxItem>
                                <ci:IconText Glyph="&#xE5CB;" Text="跟随ClassIsland" />
                            </ComboBoxItem>
                            <ComboBoxItem>
                                <ci:IconText Glyph="&#xF465;" Text="明亮" />
                            </ComboBoxItem>
                            <ComboBoxItem>
                                <ci:IconText Glyph="&#xF44B;" Text="黑暗" />
                            </ComboBoxItem>
                        </ComboBox>
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xE20B;}"
                                               Content="阴影效果"
                                               Description="悬浮窗阴影效果">
                    <controls:SettingsExpanderItem.Footer>
                        <ToggleSwitch IsChecked="{Binding ViewModel.Settings.FloatingWindowShadowEnabled, Mode=TwoWay}" />
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>

                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xE7C3;}"
                                               Content="一直显示拖动把手"
                                               Description="即使未检测到触摸设备也显示拖动把手">
                    <controls:SettingsExpanderItem.Footer>
                        <ToggleSwitch IsChecked="{Binding ViewModel.Settings.FloatingWindowDragHandleAlwaysVisible, Mode=TwoWay}" />
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>
            </controls:SettingsExpander>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xEA2F;}"
                                       Header="悬浮窗层级"
                                       Description="悬浮窗在屏幕上的层级。置底则会显示在所有窗口的后方，置顶则将显示在所有窗口的前方。"
                                       IsExpanded="True">
                <controls:SettingsExpander.Footer>
                    <ComboBox SelectedIndex="{Binding ViewModel.Settings.FloatingWindowLayer, Mode=TwoWay}">
                        <ComboBoxItem>
                            <ci:IconText Glyph="&#xE0CB;" Text="置底" />
                        </ComboBoxItem>
                        <ComboBoxItem>
                            <ci:IconText Glyph="&#xE197;" Text="置顶" />
                        </ComboBoxItem>
                    </ComboBox>
                </controls:SettingsExpander.Footer>
                <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xE125;}"
                                               Content="层级设置频率"
                                               Description="在什么时候重新设置悬浮窗层级。请注意，较高的频率可能会产生较高的性能占用并导致悬浮窗闪烁。">
                    <controls:SettingsExpanderItem.Footer>
                        <ComboBox
                            SelectedIndex="{Binding ViewModel.Settings.FloatingWindowLayerRecheckMode, Mode=TwoWay}">
                            <ComboBoxItem>窗口层级变化时</ComboBoxItem>
                            <ComboBoxItem>前台窗口变化时</ComboBoxItem>
                            <ComboBoxItem>
                                <StackPanel Orientation="Horizontal"
                                            Classes="items-valign-center"
                                            Spacing="4">
                                    <TextBlock Text="每 50ms" />
                                    <ci:FluentIcon Glyph="&#xF431;"
                                                   FontSize="20"
                                                   Margin="0 -8"
                                                   Foreground="Orange" />
                                </StackPanel>
                            </ComboBoxItem>
                            <ComboBoxItem>
                                <StackPanel Orientation="Horizontal"
                                            Classes="items-valign-center"
                                            Spacing="4">
                                    <TextBlock Text="每 1ms" />
                                    <ci:FluentIcon Glyph="&#xF431;"
                                                   FontSize="20"
                                                   Margin="0 -8"
                                                   Foreground="OrangeRed" />
                                </StackPanel>
                            </ComboBoxItem>
                        </ComboBox>
                    </controls:SettingsExpanderItem.Footer>
                </controls:SettingsExpanderItem>
            </controls:SettingsExpander>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xE9A8;}"
                                       Header="悬浮窗规则集"
                                       Description="根据规则集条件控制悬浮窗显示或隐藏"
                                       IsExpanded="False">
                <StackPanel Margin="12,8,12,12" Spacing="8">
                    <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xE9A8;}"
                                                   Content="启用规则集"
                                                   Description="启用后，当规则集不满足时悬浮窗将自动隐藏">
                        <controls:SettingsExpanderItem.Footer>
                            <ToggleSwitch IsChecked="{Binding ViewModel.Settings.FloatingWindowRulesetEnabled, Mode=TwoWay}" />
                        </controls:SettingsExpanderItem.Footer>
                    </controls:SettingsExpanderItem>
                    <ruleset:RulesetControl Ruleset="{Binding ViewModel.Settings.FloatingWindowRuleset}"
                                            ShowTitle="False"
                                            IsEnabled="{Binding ViewModel.Settings.FloatingWindowRulesetEnabled}" />
                </StackPanel>
            </controls:SettingsExpander>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xEA37;}"
                                       Header="悬浮窗按钮布局"
                                       Description="您可在下方通过拖拽调整行内按钮顺序，或添加行以实现纵列排布等功能"
                                       IsExpanded="True">
                <StackPanel Margin="12,8,12,12" Spacing="8">
                    <Grid>
                        <Button Content="添加行" Width="92" HorizontalAlignment="Left"
                                Click="OnAddFloatingTriggerRowClick" />
                    </Grid>
                    <ItemsControl ItemsSource="{Binding ViewModel.FloatingTriggerRows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="8"
                                        Margin="0,0,0,6"
                                        DragDrop.AllowDrop="True"
                                        DragDrop.DragOver="OnFloatingTriggerRowDragOver"
                                        DragDrop.Drop="OnFloatingTriggerRowDrop">
                                    <StackPanel Spacing="6">
                                        <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                                            <TextBlock Text="按钮行"
                                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                            <Button Grid.Column="1" Content="删除行"
                                                    Click="OnRemoveFloatingTriggerRowClick"
                                                    Theme="{StaticResource TransparentButton}" />
                                        </Grid>
                                        <Border MinHeight="52"
                                                CornerRadius="6"
                                                Background="{DynamicResource SubtleFillColorTertiaryBrush}"
                                                Padding="6">
                                            <ItemsControl ItemsSource="{Binding Buttons}"
                                                          HorizontalAlignment="Stretch"
                                                          VerticalAlignment="Center"
                                                          DragDrop.AllowDrop="True"
                                                          DragDrop.DragOver="OnFloatingTriggerRowDragOver"
                                                          DragDrop.Drop="OnFloatingTriggerRowDrop">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"
                                                                    HorizontalAlignment="Center" Spacing="14" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border
                                                            Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                                            CornerRadius="6"
                                                            Padding="8,4"
                                                            Margin="0,0,2,0"
                                                            DragDrop.AllowDrop="True"
                                                            DragDrop.DragOver="OnFloatingTriggerItemDragOver"
                                                            DragDrop.Drop="OnFloatingTriggerItemDrop">
                                                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                                                <Border Grid.Column="0"
                                                                        Tag="{Binding ButtonId}"
                                                                        Padding="6,2"
                                                                        CornerRadius="4"
                                                                        Background="{DynamicResource SubtleFillColorTertiaryBrush}"
                                                                        PointerPressed="OnFloatingTriggerItemPointerPressed"
                                                                        PointerMoved="OnFloatingTriggerItemPointerMoved"
                                                                        PointerReleased="OnFloatingTriggerItemPointerReleased">
                                                                    <TextBlock Text="⋮⋮"
                                                                               FontSize="14"
                                                                               VerticalAlignment="Center"
                                                                               HorizontalAlignment="Center" />
                                                                </Border>
                                                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                                                    <TextBlock Text="{Binding Icon}" />
                                                                    <TextBlock Text="{Binding ButtonName}" />
                                                                </StackPanel>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </controls:SettingsExpander>

            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xE9A8;}"
                                       Header="悬浮窗按钮配置"
                                       Description="配置每个触发按钮的显示状态和规则集"
                                       IsExpanded="False">
                <StackPanel Margin="12,8,12,12" Spacing="8">
                    <ItemsControl ItemsSource="{Binding ViewModel.FloatingTriggerButtonConfigs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="12"
                                        Margin="0,0,0,8">
                                    <StackPanel Spacing="8">
                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                            <TextBlock Grid.Column="0" Text="{Binding Icon}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <TextBlock Grid.Column="1" Text="{Binding ButtonName}" VerticalAlignment="Center" FontWeight="SemiBold" />
                                            <ToggleSwitch Grid.Column="2"
                                                          OnContent="显示" OffContent="隐藏"
                                                          IsChecked="{Binding Config.IsVisible, Mode=TwoWay}" />
                                        </Grid>
                                        <controls:SettingsExpanderItem IconSource="{ci:FluentIconSource &#xE9A8;}"
                                                                       Content="启用规则集"
                                                                       Description="启用后，当规则集不满足时此按钮将自动隐藏">
                                            <controls:SettingsExpanderItem.Footer>
                                                <ToggleSwitch IsChecked="{Binding Config.RulesetEnabled, Mode=TwoWay}" />
                                            </controls:SettingsExpanderItem.Footer>
                                        </controls:SettingsExpanderItem>
                                        <ruleset:RulesetControl Ruleset="{Binding Config.Ruleset}"
                                                                ShowTitle="False"
                                                                IsEnabled="{Binding Config.RulesetEnabled}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </controls:SettingsExpander>

            <TextBlock Text="Programmer_Wang ©2026"
                       Foreground="Gray"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,40,0,0"
                       Opacity="0.7" />
        </StackPanel>
    </ScrollViewer>
</ci:SettingsPageBase>
